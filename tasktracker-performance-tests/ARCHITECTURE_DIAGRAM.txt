```
╔════════════════════════════════════════════════════════════════════════════════╗
║                    TASKTRACKER PERFORMANCE TESTING SUITE                        ║
║                         Architecture Comparison Tool                            ║
╚════════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│                            TESTING ARCHITECTURE                               │
└──────────────────────────────────────────────────────────────────────────────┘

                           ┌─────────────────────┐
                           │  Locust Test Runner │
                           │   (Your Computer)   │
                           └──────────┬──────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    │                                   │
          ┌─────────▼─────────┐              ┌─────────▼─────────┐
          │   MONOLITHIC APP  │              │ MICROSERVICES APP │
          │   Port: 9000      │              │   Port: 8000      │
          │   DB: 5435        │              │   API Gateway     │
          └─────────┬─────────┘              └─────────┬─────────┘
                    │                                   │
                    │                        ┌──────────┴──────────┐
                    │                        │                     │
          ┌─────────▼─────────┐    ┌────────▼────────┐  ┌────────▼────────┐
          │   PostgreSQL      │    │  User Service   │  │  Task Service   │
          │   (1 Database)    │    │    Port: 8001   │  │    Port: 8002   │
          └───────────────────┘    │    DB: 5433     │  │    DB: 5434     │
                                   └─────────────────┘  └─────────────────┘
                                            │                     │
                                   ┌────────▼─────────────────────▼────┐
                                   │      Stats Service                │
                                   │        Port: 8003                 │
                                   │     (Calls Task Service)          │
                                   └───────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│                              TEST FLOW DIAGRAM                                │
└──────────────────────────────────────────────────────────────────────────────┘

    1. SETUP PHASE (Automatic)
       ├── Create 100 users
       ├── Login each user (get JWT tokens)
       └── Create 20 tasks per user (2000 tasks total)
                    │
                    ▼
    2. RAMP-UP PHASE
       └── Spawn users at configured rate (default: 10/sec)
                    │
                    ▼
    3. LOAD TESTING PHASE
       ├── Each user performs weighted operations:
       │   ├── 70% Read   (GET tasks, stats, user info)
       │   ├── 20% Write  (POST create tasks)
       │   ├── 7%  Update (PUT/PATCH update, complete)
       │   └── 3%  Delete (DELETE tasks)
       │
       └── Wait 1-3 seconds between requests
                    │
                    ▼
    4. RESULTS COLLECTION
       ├── Response times (per endpoint)
       ├── Throughput (requests/sec)
       ├── Failure rates
       └── Time-series data
                    │
                    ▼
    5. ANALYSIS & VISUALIZATION
       ├── Generate comparison charts (5 charts)
       ├── Create summary table
       └── Export HTML reports

┌──────────────────────────────────────────────────────────────────────────────┐
│                          WHAT GETS TESTED (APIs)                              │
└──────────────────────────────────────────────────────────────────────────────┘

    AUTHENTICATION (3 endpoints)
    ├── POST /api/v1/auth/register    ← User registration
    ├── POST /api/v1/auth/login       ← Get JWT token
    └── GET  /api/v1/auth/me          ← Get user info

    TASK MANAGEMENT (8 endpoints)
    ├── POST   /api/v1/tasks/                    ← Create task
    ├── GET    /api/v1/tasks/                    ← List all tasks
    ├── GET    /api/v1/tasks/?status=...         ← List filtered
    ├── GET    /api/v1/tasks/{id}                ← Get single task
    ├── PUT    /api/v1/tasks/{id}                ← Update task
    ├── PATCH  /api/v1/tasks/{id}/complete       ← Mark complete
    ├── PATCH  /api/v1/tasks/{id}/incomplete     ← Mark incomplete
    └── DELETE /api/v1/tasks/{id}                ← Delete task

    STATISTICS (1 endpoint)
    └── GET /api/v1/stats/                       ← User statistics

┌──────────────────────────────────────────────────────────────────────────────┐
│                           GENERATED OUTPUTS                                   │
└──────────────────────────────────────────────────────────────────────────────┘

    results/
    ├── monolithic_20231213_120000/
    │   ├── report.html              ← Interactive web report
    │   ├── stats_stats.csv          ← Raw statistics data
    │   └── locust.log              ← Test execution log
    │
    ├── microservices_20231213_120000/
    │   ├── report.html              ← Interactive web report
    │   ├── stats_stats.csv          ← Raw statistics data
    │   └── locust.log              ← Test execution log
    │
    └── comparison_20231213_120000/
        ├── response_time_comparison.png      ← Chart 1
        ├── throughput_comparison.png         ← Chart 2
        ├── failure_rate_comparison.png       ← Chart 3
        ├── percentile_comparison.png         ← Chart 4
        ├── summary_table.png                 ← Chart 5
        └── summary.txt                       ← Text summary

┌──────────────────────────────────────────────────────────────────────────────┐
│                          USAGE - QUICK COMMANDS                               │
└──────────────────────────────────────────────────────────────────────────────┘

    # First time setup
    ./setup.sh

    # Verify everything is ready
    ./verify_setup.sh

    # Run both tests in parallel (RECOMMENDED)
    ./run_both_tests.sh

    # Interactive menu
    ./run_tests.sh

    # Custom configuration
    USERS=100 RUN_TIME=5m ./run_both_tests.sh

    # View results
    open results/comparison_*/summary_table.png
    open results/monolithic_*/report.html
    open results/microservices_*/report.html

┌──────────────────────────────────────────────────────────────────────────────┐
│                        KEY PERFORMANCE METRICS                                │
└──────────────────────────────────────────────────────────────────────────────┘

    Response Time                  Throughput
    ├── Average                    ├── Requests per second
    ├── Minimum                    ├── Total requests
    ├── Maximum                    └── Failed requests
    ├── 50th Percentile (median)
    ├── 95th Percentile           Reliability
    ├── 99th Percentile           ├── Failure rate %
    └── 99.9th Percentile         ├── Error types
                                  └── Success rate %

┌──────────────────────────────────────────────────────────────────────────────┐
│                         EXPECTED RESULTS                                      │
└──────────────────────────────────────────────────────────────────────────────┘

    MONOLITHIC                         MICROSERVICES
    ├── Lower Latency                  ├── Higher Latency
    ├── Higher Throughput              ├── Lower Throughput
    ├── Simpler Path                   ├── Multiple Network Hops
    ├── Single Database                ├── Multiple Databases
    └── Avg: 40-60ms                   └── Avg: 70-100ms

    Trade-offs:
    Mono:  ✅ Performance  ❌ Scalability  ❌ Fault Isolation
    Micro: ❌ Performance  ✅ Scalability  ✅ Fault Isolation

┌──────────────────────────────────────────────────────────────────────────────┐
│                          TEST CONFIGURATIONS                                  │
└──────────────────────────────────────────────────────────────────────────────┘

    Quick Test       → USERS=30  RUN_TIME=30s  (baseline)
    Standard Load    → USERS=50  RUN_TIME=60s  (default)
    Medium Load      → USERS=100 RUN_TIME=5m   (normal usage)
    Stress Test      → USERS=200 RUN_TIME=10m  (high load)
    Endurance Test   → USERS=100 RUN_TIME=30m  (stability)

┌──────────────────────────────────────────────────────────────────────────────┐
│                              FILE STRUCTURE                                   │
└──────────────────────────────────────────────────────────────────────────────┘

    tasktracker-performance-tests/
    │
    ├── 📋 Test Files
    │   ├── locustfile_monolithic.py       (Mono tests)
    │   ├── locustfile_microservices.py    (Micro tests)
    │   ├── config.py                      (Configuration)
    │   └── utils.py                       (Utilities)
    │
    ├── 📊 Analysis
    │   └── analyze_results.py             (Chart generation)
    │
    ├── 🔧 Scripts (8 executable)
    │   ├── setup.sh
    │   ├── verify_setup.sh
    │   ├── run_mono_test.sh
    │   ├── run_micro_test.sh
    │   ├── run_both_tests.sh              ⭐ Main script
    │   ├── run_tests.sh
    │   ├── compare_results.sh
    │   └── cleanup.sh
    │
    └── 📚 Documentation
        ├── README.md                      (Full documentation)
        ├── QUICKSTART.md                  (Quick guide)
        ├── PROJECT_SUMMARY.md             (Overview)
        ├── COMPLETION_SUMMARY.md          (This completed)
        └── ARCHITECTURE_DIAGRAM.txt       (This file)

═══════════════════════════════════════════════════════════════════════════════
                           🎉 READY TO TEST! 🎉
═══════════════════════════════════════════════════════════════════════════════

Quick Start:
    1. ./setup.sh
    2. Start both apps (mono on 9000, micro on 8000)
    3. ./run_both_tests.sh
    4. View results in results/comparison_*/

For help: cat README.md
```

