# Experiment dependencies
# Most dependencies should already be satisfied by tasktracker-performance-tests

# Plotting
matplotlib>=3.8.0

# Already required by performance tests (listed for completeness)
locust>=2.20.0
requests>=2.31.0
faker>=22.0.0

# Optional: Docker SDK (alternative to subprocess docker commands)
# docker>=7.0.0
